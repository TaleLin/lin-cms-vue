# <%= title %>（<%= name %>）

`<%= title %>` 插件用于 <%= description || '补充具体业务场景' %>。

## 开发基线

- 前端运行于当前主仓的 Vue 3.5 + Vite + Element Plus 环境
- 页面与组件默认使用 `<script setup>`
- 插件路由和导航入口由 `stage-config.js` 管理

## 入口

- 舞台页面 1：`/<%= name %>/stage1`
- 舞台页面 2：`/<%= name %>/stage2`
- 共享组件：`component/component.vue`

## 开发约定

- 页面级状态、路由交互和提交流程优先保留在页面组件内
- 只有共享副作用或共享领域逻辑才抽成 `use-*.js`
- 纯校验、映射和格式化逻辑放到 `*-helpers.js`
